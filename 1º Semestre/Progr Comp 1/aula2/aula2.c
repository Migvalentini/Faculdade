#include <stdio.h>; 
#include <math.h>;

main() {
  // Operadores Matemáticos
  // Soma +
  // Subtração -
  // Multiplicação *
  // Divisão /
  // Resto da divisão %

  // Formatadores
  // Inteiro %d
  // Reais %f
  // Caracteres %c
  // Quebra de Linha \n

  printf("Mensagem \n");

  int x,y,z;
  x = 5;
  y = -9;
  z = x + y;
  
  printf("%d\n", z);
  printf("%d + %d = %d\n", x,y,z);
  scanf("%d%d",&x,&y);
}