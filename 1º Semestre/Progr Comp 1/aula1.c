#include <stdio.h>; 
#include <math.h>;

int main() {
    printf("Hello World!\n");
    if (2 <= 4)
    {
        printf("menor que ");
    }

    int a;
    
    for( a = 10; a < 20; a = a + 1 ){
      printf("value of a: %d\n", a);
    }

}